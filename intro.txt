introduccion javascript
//comentarios de una linea
/*
comentario multiple
*/
//variables -> let var siempre utilizar let numero = 1; mejor.
//constantes const HOST = "localhost"

/*
funtion sumar(){
    console.log("sumando")
    return num1 + num2
}

console.log(sumar(1, 2))

//condicion
if(numero > 1){
    // si se cumple la condicion
}else if (numero < 0 || numero > -2){
    //si se cumple el else if
}else {
    //si no se cumple la funcion
}
*/



config ={
    'host': '',
    'port': 0,
    'user': '',
    'password': '',
    'database': ''
}

#** para accionar todos los elementos del diccionario config
if config['host'] != '':
    DB = mariadb.connect(**config)
    DB.autocommit = True
else:
    DB = False